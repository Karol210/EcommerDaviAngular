/**
 * Interfaz que representa la petición para actualizar el estado de un producto.
 * Solo incluye el campo necesario para cambiar el estado del producto.
 */
export interface ProductUpdateRequest {
  estadoProductoId: number;
}

